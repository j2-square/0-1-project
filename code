#include <bits/stdc++.h>
using namespace std;

string binary(string input)
{
    string output = input;

    // YOU WILL ADD your mapping logic here later
    // For now, it simply returns the same 0/1/space sequence

    return output;
}

int main()
{
    string input;
    getline(cin, input);   // reads 0,1,space exactly as typed

    string output = binary(input);
    cout << output<<endl;

    return 0;
}
